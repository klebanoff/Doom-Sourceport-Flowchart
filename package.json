{
  "name": "doom-sourceport-flowchart",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun --hot index.ts",
    "build": "bun build ./src/main.ts --outdir=dist --minify && bun scripts/copyStatic.ts",
    "typecheck": "bun x tsc --noEmit",
    "validate:data": "bun scripts/validateData.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
